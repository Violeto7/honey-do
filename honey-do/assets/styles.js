import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },

    selections: {
      
      width: '80%'
    },

    selectButton: {
      justifyContent: 'center',
      alignItems: 'center',
      margin: 8,
      borderWidth: 2,
      borderRadius: 16,
      height: '30%',
    }, 

    buttonText: {
      fontSize: 16,
    },
});

export {styles};